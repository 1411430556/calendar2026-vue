---
alwaysApply: true
scene: git_message
---

使用中文编写提交信息，必须记住提交信息的格式为：

```
<type>(<scope>): <subject>
```

其中：

- `<type>`：提交类型，可选值为 `feat`、`fix`、`docs`、`chore`、`ci`、`style`、`refactor`、`test` 等。
- `<scope>`：影响的范围，可选值为 `components`、`utils`、`styles` 等。
- `<subject>`：提交的描述，必须是中文。
